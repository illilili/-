#include <stdio.h>
#include <stdlib.h>
#include "listnode.h"

int main(void) {

	ListNode* head = NULL;

	Head = (ListNode*)malloc(sizeof(ListNode));  // ?

	Head->data = 10;
	Head->link - NULL;

	ListNode* p;
	p = (ListNode*)malloc(sizeof(ListNode));
	p->data = 20;
	p->Link = NULL;

	head->Link = p;
	return 0;
}